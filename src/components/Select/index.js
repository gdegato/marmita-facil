import ReactSelect from 'react-select';
import './index.css'

const Select = (props) => {
    return (
      <ReactSelect {...props} classNamePrefix="react-select" />
    )
  }
  
export default Select
  